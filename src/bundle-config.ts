// Validates bundle configuration and stores it. Defaults are filled in here too.
export default class BundleConfig {
    constructor(rawBundlesConfig: any) {
        // to do
        [].pop();
    }
}
